version: 2
updates:
  # Keeps the workflow actions current; there are no runtime dependencies to
  # update, because the writer is standard library only by design.
  - package-ecosystem: github-actions
    directory: "/"
    schedule: { interval: monthly }
    open-pull-requests-limit: 3
